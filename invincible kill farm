-- Define the whitelist of Usernames
local whitelistedUsers = {
    "hebdjxhc", "xntz_o", "VVSXOXlolkgg",
    "krxzy_214", "alexglover020", "ALT_killa08",
}

local player = game:GetService("Players").LocalPlayer

-- Whitelist check with instant kick
if not table.find(whitelistedUsers, player.Name) then
    player:Kick("haha you thought you could use this script? think again nerd")
    return
end

-- UI Library (keep original or change if dead)
local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/memejames/elerium-v2-ui-library/main/Library"))()
local window = library:AddWindow("Invincible Kill Farm", {
    main_color = Color3.fromRGB(0, 0, 0),
    min_size = Vector2.new(200, 200),
    can_resize = false,
    background_transparency = 1,
})

local tab = window:AddTab("credits")
tab:AddLabel("made by Invincible")
tab:AddLabel("https://discord.gg/eSVF5Rsf")

-- Services
local Players          = game:GetService("Players")
local RunService       = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualUser      = game:GetService("VirtualUser")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TeleportService  = game:GetService("TeleportService")
local HttpService      = game:GetService("HttpService")
local LocalPlayer      = Players.LocalPlayer

if getgenv().AutoKillLoaded then return end
getgenv().AutoKillLoaded = true

-- Settings
local AUTO_SERVER_HOP    = true
local SERVER_HOP_INTERVAL = 8   -- seconds
local LastServerHop       = os.clock()

local Running             = true
local WhitelistFriends    = true
local KillOnlyWeaker      = true

getgenv().WhitelistedPlayers      = getgenv().WhitelistedPlayers or {}
getgenv().TempWhitelistStronger   = getgenv().TempWhitelistStronger or {}

-- Blocked animations (unused in current logic — kept for possible future use)
local BlockedAnimations = {
    ["rbxassetid://3638729053"] = true,
    ["rbxassetid://3638749874"] = true,
    ["rbxassetid://3638767427"] = true,
    ["rbxassetid://102357151005774"] = true
}

local function GetPlayerStatValue(plr, statNames)
    if not plr then return nil end
    if type(statNames) == "string" then statNames = {statNames} end
    
    -- Try attributes first
    for _, name in statNames do
        local val = plr:GetAttribute(name)
        if val ~= nil then return tonumber(val) end
    end
    
    -- Then leaderstats
    local stats = plr:FindFirstChild("leaderstats")
    if stats then
        for _, name in statNames do
            local v = stats:FindFirstChild(name)
            if v and v:IsA("ValueBase") then return tonumber(v.Value) end
        end
    end
    return nil
end

local function GetLocalPlayerDamage()
    return GetPlayerStatValue(LocalPlayer, {"Damage", "DMG", "Attack", "Strength", "Str"}) or 1
end

local function GetTargetHealth(targetPlayer)
    local hum = targetPlayer.Character and targetPlayer.Character:FindFirstChildOfClass("Humanoid")
    return hum and hum.MaxHealth or 100
end

local function ShouldKillPlayer(targetPlayer)
    if not KillOnlyWeaker then return true end
    
    local myDmg   = GetLocalPlayerDamage()
    local theirHP = GetTargetHealth(targetPlayer)
    local hitsNeeded = math.ceil(theirHP / myDmg)
    
    return hitsNeeded <= 5
end

-- Character update
local Character, Humanoid, Hand, Punch
local LastAttack = 0

local function UpdateAll()
    Character = LocalPlayer.Character
    if not Character then return end
    
    Humanoid = Character:FindFirstChildOfClass("Humanoid")
    Hand     = Character:FindFirstChild("LeftHand") or Character:FindFirstChild("Left Arm")
    Punch    = Character:FindFirstChild("Punch")
end

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1)
    UpdateAll()
end)

UpdateAll()

-- Anti-idle
LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

-- Better server hop
local function ServerHop()
    local placeId = game.PlaceId
    local jobId   = game.JobId
    
    local success, response = pcall(function()
        return HttpService:JSONDecode(game:HttpGet(
            "https://games.roblox.com/v1/games/" .. placeId .. "/servers/Public?sortOrder=Desc&limit=100"
        ))
    end)
    
    if not success or not response or not response.data then
        warn("Server list fetch failed")
        return
    end
    
    for _, server in ipairs(response.data) do
        if server.id == jobId then continue end
        if not server.playing or server.playing < 10 then continue end
        
        local fill = server.playing / server.maxPlayers
        if fill >= 0.6 then
            print("Hopping to server:", server.id, "(" .. server.playing .. "/" .. server.maxPlayers .. ")")
            TeleportService:TeleportToPlaceInstance(placeId, server.id, LocalPlayer)
            return
        end
    end
    
    warn("No suitable server found")
end

-- Main loop
RunService.RenderStepped:Connect(function()
    -- Server hop timer
    if AUTO_SERVER_HOP and (os.clock() - LastServerHop) >= SERVER_HOP_INTERVAL then
        LastServerHop = os.clock()
        task.spawn(ServerHop)  -- async hop
    end
    
    if not Running or not Character or not Humanoid or Humanoid.Health <= 0 then
        return
    end
    
    -- Equip punch if missing
    if not Punch then
        local tool = LocalPlayer.Backpack:FindFirstChild("Punch")
        if tool then
            Humanoid:EquipTool(tool)
        end
        task.wait(0.2)
        UpdateAll()
        return
    end
    
    if (os.clock() - LastAttack) < 0.08 then return end
    LastAttack = os.clock()
    
    -- Spam attack (adjust delay if needed)
    if Punch:FindFirstChild("attackTime") then
        Punch.attackTime.Value = 0
    end
    Punch:Activate()
    
    -- Kill loop
    for _, otherPlayer in ipairs(Players:GetPlayers()) do
        if otherPlayer == LocalPlayer then continue end
        
        -- Add your friend/stronger player whitelist logic here if needed
        if WhitelistFriends and otherPlayer:IsFriendsWith(LocalPlayer.UserId) then continue end
        
        if not ShouldKillPlayer(otherPlayer) then continue end
        
        local char = otherPlayer.Character
        if not char then continue end
        
        local hum  = char:FindFirstChildOfClass("Humanoid")
        local head = char:FindFirstChild("Head")
        local root = char:FindFirstChild("HumanoidRootPart")
        
        if hum and head and root and hum.Health > 0 then
            local wasAnchored = root.Anchored
            root.Anchored = true  -- helps touch register
            
            firetouchinterest(head, Hand, 0)
            task.wait()
            firetouchinterest(head, Hand, 1)
            
            root.Anchored = wasAnchored
        end
    end
end)

-- Optional: Attempt to walk to Muscle King Gym area (approximate coords - test in-game!)
-- These coords are guesses — open F3X or explorer to get real ones
local function tryMoveToMuscleKingGym()
    if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        return
    end
    
    local hrp = LocalPlayer.Character.HumanoidRootPart
    -- Example position near Muscle King Gym portal / area (you need to find real one)
    local targetPos = Vector3.new(-8626, 16, -5730)  -- ← your original guess; likely wrong
    
    -- Simple walk (replace with better pathfinding if needed)
    LocalPlayer.Character.Humanoid:MoveTo(targetPos)
    
    -- Or force CFrame (risky, anti-cheat may detect)
    -- hrp.CFrame = CFrame.new(targetPos + Vector3.new(0, 5, 0))
end

-- Run once after load (you can toggle this or bind to key)
task.delay(5, function()
    tryMoveToMuscleKingGym()
end)

print("Script loaded - Kill farm active | Server hop:", AUTO_SERVER_HOP)
