-- Define the whitelist of Usernames
local whitelistedUsers = {
    "hebdjxhc", "xntz_o",
    "krxzy_214", "alexglover020", "ALT_killa08",
}

local player = game:GetService("Players").LocalPlayer
local rs = game:GetService("ReplicatedStorage")
local workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")

-- Whitelist check with instant kick
if not table.find(whitelistedUsers, player.Name) then
    player:Kick("haha you thought you could use this script? think again nerd")
    return
end

local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/memejames/elerium-v2-ui-library/main/Library"))()

local window = library:AddWindow("Invincible Kill Farm", {
    main_color = Color3.fromRGB(0, 0, 0),
    min_size = Vector2.new(200, 200),
    can_resize = false,
    background_transparency = 1,
})

local tab = window:AddTab("credits")
tab:AddLabel("made by Invincible")
tab:AddLabel("https://discord.gg/eSVF5Rsf")

-- Stats tab for kill counter
local statsTab = window:AddTab("Stats")
local KillCounterLabel = statsTab:AddLabel("Kills: 0")
local KillCount = 0

local function UpdateKillCount()
    KillCounterLabel.Text = "Kills: " .. KillCount
end

local Character, Humanoid, Hand, Punch
local LastAttack = 0
local Running = true

local function SetupDeathListener(player)
    player.CharacterAdded:Connect(function()
        local character = player.Character
        local hum = character and character:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.Died:Connect(function()
                -- Increment kill count when this player dies
                KillCount = KillCount + 1
                UpdateKillCount()
            end)
        end
    end)
end

-- Initialize death listeners for existing players
for _, player in ipairs(Players:GetPlayers()) do
    if player ~= LocalPlayer then
        SetupDeathListener(player)
    end
end

-- Listen for new players joining
Players.PlayerAdded:Connect(function(player)
    if player ~= LocalPlayer then
        SetupDeathListener(player)
    end
end)

-- Teleport to Muscle King Gym
local function TeleportToMuscleKingGym()
    local gymPart = workspace:FindFirstChild("MuscleKingGym")
    if gymPart and gymPart:IsA("BasePart") then
        -- Teleport your character to the gym's position
        LocalPlayer.Character:PivotTo(gymPart.CFrame * CFrame.new(0, 3, 0))
    else
        -- Alternatively, teleport to specific coordinates
        -- LocalPlayer.Character:PivotTo(CFrame.new(x, y, z))
        print("Gym part not found, please set the correct part or coordinates.")
    end
end

TeleportToMuscleKingGym()

-- Main attack loop
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1)
    UpdateAll()
end)

UpdateAll()

LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

local function GetPlayerStatValue(Player, StatNames)
    if not Player then return nil end
    if type(StatNames) == "string" then StatNames = {StatNames} end

    for _, Name in ipairs(StatNames) do
        local Attr = Player:GetAttribute(Name)
        if Attr ~= nil then return tonumber(Attr) end
    end

    local stats = Player:FindFirstChild("leaderstats")
    if stats then
        for _, Name in ipairs(StatNames) do
            local v = stats:FindFirstChild(Name)
            if v then return tonumber(v.Value) end
        end
    end
    return nil
end

local function GetLocalPlayerDamage()
    return GetPlayerStatValue(LocalPlayer, {"Damage","DMG","Attack","Strength","Str"}) or 1
end

local function GetTargetHealth(Player)
    local hum = Player.Character and Player.Character:FindFirstChildOfClass("Humanoid")
    return hum and hum.MaxHealth or 100
end

local function UpdateAll()
    Character = LocalPlayer.Character
    Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
    Hand = Character and (Character:FindFirstChild("LeftHand") or Character:FindFirstChild("Left Arm"))
    Punch = Character and Character:FindFirstChild("Punch")
end

local function ShouldKillPlayer(player)
    if not KillOnlyWeaker then return true end
    local hits = math.ceil(GetTargetHealth(player) / (GetPlayerStatValue(player, {"Damage","DMG","Attack","Strength","Str"}) or 1))
    return hits <= 5
end

-- Setup death detection for enemies
local function SetupKillDetection(enemyHumanoid, enemyCharacter, player)
    enemyHumanoid.Died:Connect(function()
        -- Check if the last attacker was you
        local creatorTag = enemyHumanoid:FindFirstChild("creator")
        if creatorTag and creatorTag.Value == LocalPlayer then
            KillCount = KillCount + 1
            UpdateKillCount()
        end
    end)
end

-- Initialize detection for existing enemies
RunService.Heartbeat:Connect(function()
    for _, enemy in ipairs(workspace:GetChildren()) do
        if enemy:FindFirstChildOfClass("Humanoid") and enemy ~= Character then
            local hum = enemy:FindFirstChildOfClass("Humanoid")
            if not hum:GetAttribute("KillSetup") then
                hum:SetAttribute("KillSetup", true)
                SetupKillDetection(hum, enemy, LocalPlayer)
            end
        end
    end
end)

-- Attack loop
RunService.RenderStepped:Connect(function()
    -- SERVER HOP
    if AUTO_SERVER_HOP and os.clock() - LastServerHop >= SERVER_HOP_INTERVAL then
        LastServerHop = os.clock()
        ServerHop()
        return
    end

    if not Running or not Character or not Humanoid then return end
    if os.clock() - LastAttack < 0.05 then return end
    LastAttack = os.clock()

    if not Punch then
        local tool = LocalPlayer.Backpack:FindFirstChild("Punch")
        if tool then
            Humanoid:EquipTool(tool)
        end
        Punch = Character:FindFirstChild("Punch")
        return
    end

    Punch.attackTime.Value = 0
    Punch:Activate()

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and ShouldKillPlayer(player) then
            local char = player.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local head = char and char:FindFirstChild("Head")
            local root = char and char:FindFirstChild("HumanoidRootPart")

            if hum and head and root and hum.Health > 0 then
                local wasAnchored = root.Anchored
                root.Anchored = true
                firetouchinterest(head, Hand, 0)
                firetouchinterest(head, Hand, 1)
                root.Anchored = wasAnchored
            end
        end
    end
end)
