-- Place this as a LocalScript inside StarterPlayer > StarterPlayerScripts

local Player = game.Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local Lighting = game:GetService("Lighting")
local Players = game:GetService("Players")
local LocalChar = Player.Character or Player.CharacterAdded:Wait()

-- UI Setup
local gui = Instance.new("ScreenGui", PlayerGui)
gui.Name = "MuscleLegendsUI"

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0, 700, 0, 600)
frame.Position = UDim2.new(0.5, -350, 0.5, -300)
frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
frame.BorderSizePixel = 0

local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1, 0, 0, 50)
title.BackgroundColor3 = Color3.fromRGB(50, 0, 0)
title.TextColor3 = Color3.new(1, 1, 1)
title.Text = "Muscle Legends Automation"
title.Font = Enum.Font.SourceSansBold
title.TextSize = 24

local yOffset = 60

local function createButton(text, y)
    local btn = Instance.new("TextButton", frame)
    btn.Size = UDim2.new(0, 350, 0, 50)
    btn.Position = UDim2.new(0, 20, 0, y)
    btn.Text = text
    btn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 20
    return btn
end

local function createInput(text, y)
    local input = Instance.new("TextBox", frame)
    input.Size = UDim2.new(0, 350, 0, 50)
    input.Position = UDim2.new(0, 370, 0, y)
    input.PlaceholderText = text
    input.Text = "30"
    input.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    input.TextColor3 = Color3.new(1, 1, 1)
    input.Font = Enum.Font.SourceSansBold
    input.TextSize = 20
    return input
end

-- UI Elements
local btnTPGym = createButton("TP to Gym", yOffset)
local btnSetNight = createButton("Set Night Time", yOffset + 60)
local btnAutoKill = createButton("Start Auto Kill", yOffset + 120)
local btnStopAutoKill = createButton("Stop Auto Kill", yOffset + 180)
local btnServerHop = createButton("Server Hop", yOffset + 240)
local btnToggleFarm = createButton("Toggle Auto Farm", yOffset + 300)
local btnToggleServerHop = createButton("Toggle Server Hop", yOffset + 360)
local intervalInput = createInput("Server Hop Interval (s)", yOffset + 420)
intervalInput.Text = "30"

-- Toggle button for "Kill Only Weaker"
local killOnlyWeaker = true
local killToggleBtn = Instance.new("TextButton", frame)
killToggleBtn.Size = UDim2.new(0, 50, 0, 50)
killToggleBtn.Position = UDim2.new(0, 20, 0, yOffset + 480)
killToggleBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
killToggleBtn.BorderSizePixel = 0
killToggleBtn.Text = "☑"
killToggleBtn.TextColor3 = Color3.new(1, 1, 1)
killToggleBtn.Font = Enum.Font.SourceSansBold
killToggleBtn.TextSize = 24

local killLabel = Instance.new("TextLabel", frame)
killLabel.Size = UDim2.new(0, 250, 0, 50)
killLabel.Position = UDim2.new(0, 80, 0, yOffset + 480)
killLabel.BackgroundTransparency = 1
killLabel.Text = "Kill Only Weaker"
killLabel.TextColor3 = Color3.white
killLabel.Font = Enum.Font.SourceSansBold
killLabel.TextSize = 20

-- Variables
local autoFarm = false
local autoKillRunning = false
local autoKillConnection
local serverHopEnabled = false
local lastServerHopTime = os.clock()
local serverHopInterval = tonumber(intervalInput.Text) or 30

-- Functions
local function setNight()
    Lighting.ClockTime = 0
    Lighting.Brightness = 0.5
    Lighting.Ambient = Color3.fromRGB(50, 50, 70)
    Lighting.OutdoorAmbient = Color3.fromRGB(30, 30, 50)
    print("Night time set")
end

local function tpToGym()
    local hrp = LocalChar:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.CFrame = CFrame.new(-8625.93262, 17.2325287, -5730.47217)
        print("Teleported to Gym")
    end
end

local function getPlayerDamage()
    local attr = LocalChar:GetAttribute("Damage") or LocalChar:GetAttribute("DMG") or LocalChar:GetAttribute("Attack") or LocalChar:GetAttribute("Strength") or LocalChar:GetAttribute("Str")
    if attr then
        return tonumber(attr)
    end
    local stats = LocalChar:FindFirstChild("leaderstats")
    if stats then
        for _, v in pairs(stats:GetChildren()) do
            if v.Name:lower():find("damage") or v.Name:lower():find("attack") or v.Name:lower():find("str") then
                return tonumber(v.Value)
            end
        end
    end
    return 1 -- default damage
end

local function getTargetHealth(target)
    local hum = target.Character and target.Character:FindFirstChildOfClass("Humanoid")
    return hum and hum.MaxHealth or 100
end

local function shouldKill(targetPlayer)
    if not killOnlyWeaker then return true end
    local damage = getPlayerDamage()
    local health = getTargetHealth(targetPlayer)
    return math.ceil(health / damage) <= 5
end

local lastAttackTime = 0
local function attackNearby()
    if os.clock() - lastAttackTime < 0.1 then return end
    lastAttackTime = os.clock()

    -- Activate Punch
    local punch = LocalChar:FindFirstChild("Punch")
    if punch then punch:Activate() end

    for _, other in pairs(Players:GetPlayers()) do
        if other ~= Player and other.Character then
            if shouldKill(other) then
                local head = other.Character:FindFirstChild("Head")
                local root = other.Character:FindFirstChild("HumanoidRootPart")
                local hum = other.Character:FindFirstChildOfClass("Humanoid")
                if head and root and hum and hum.Health > 0 then
                    local wasAnchored = root.Anchored
                    root.Anchored = true
                    local leftHand = LocalChar:FindFirstChild("LeftHand") or LocalChar:FindFirstChild("Left Arm")
                    if leftHand then
                        firetouchinterest(head, leftHand, 0)
                        task.wait(0.1)
                        firetouchinterest(head, leftHand, 1)
                        root.Anchored = wasAnchored
                    end
                end
            end
        end
    end
end

local function startAutoKill()
    if autoKillRunning then return end
    autoKillRunning = true
    autoKillConnection = RunService.Heartbeat:Connect(function()
        if not autoFarm then return end
        local hum = LocalChar:FindFirstChildOfClass("Humanoid")
        if hum and hum.Health > 0 then
            if not LocalChar:FindFirstChild("Punch") then
                local punchTool = Player.Backpack:FindFirstChild("Punch")
                if punchTool then
                    hum:EquipTool(punchTool)
                end
            else
                attackNearby()
            end
        end
    end)
    print("Auto kill started")
end

local function stopAutoKill()
    if autoKillConnection then autoKillConnection:Disconnect() end
    autoKillRunning = false
    print("Auto kill stopped")
end

local function serverHop()
    print("Hopping server...")
    local placeId = game.PlaceId
    local myJobId = game.JobId
    for attempt=1,6 do
        local success, response = pcall(function()
            return HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..placeId.."/servers/Public?sortOrder=Desc&limit=100"))
        end)
        if not success or not response or not response.data then wait(2 + attempt) continue end

        local candidates = {}
        for _, srv in pairs(response.data) do
            if srv.id == myJobId then continue end
            if srv.playing >= srv.maxPlayers then continue end
            if srv.playing < 8 then continue end
            local fill = srv.playing / srv.maxPlayers
            if fill > 0.25 and fill < 0.9 then
                table.insert(candidates, srv)
            end
        end

        if #candidates > 0 then
            local target = candidates[math.random(1, #candidates)]
            pcall(function()
                TeleportService:TeleportToPlaceInstance(placeId, target.id, Player)
            end)
            break
        else
            wait(3 + attempt)
        end
    end
end

-- Button connections
btnTPGym.MouseButton1Click:Connect(tpToGym)
btnSetNight.MouseButton1Click:Connect(setNight)
btnAutoKill.MouseButton1Click:Connect(function()
    autoFarm = true
    startAutoKill()
end)
btnStopAutoKill.MouseButton1Click:Connect(function()
    autoFarm = false
    stopAutoKill()
end)
btnServerHop.MouseButton1Click:Connect(serverHop)

intervalInput.FocusLost:Connect(function()
    local val = tonumber(intervalInput.Text)
    if val then
        serverHopInterval = val
        print("Server hop interval set to", val)
    end
end)

-- Toggle server hop
local function toggleServerHop()
    serverHopEnabled = not serverHopEnabled
    print("Server Hop toggled:", serverHopEnabled)
end
createButton("Toggle Server Hop", yOffset + 420).MouseButton1Click:Connect(toggleServerHop)

-- Toggle Kill Only Weaker
killToggleBtn.MouseButton1Click:Connect(function()
    killOnlyWeaker = not killOnlyWeaker
    if killOnlyWeaker then
        killToggleBtn.Text = "☑"
    else
        killToggleBtn.Text = "☐"
    end
    print("Kill Only Weaker:", killOnlyWeaker)
end)

-- Main loop for server hop timing
RunService.Heartbeat:Connect(function()
    if serverHopEnabled then
        if os.clock() - lastServerHopTime >= serverHopInterval then
            lastServerHopTime = os.clock()
            serverHop()
        end
    end
end)

-- Auto set night & big hitbox
_G.AutoSetNight = false
_G.AutoBigHitbox = false
_G.AutoLockPosition = false

RunService.Heartbeat:Connect(function()
    if _G.AutoSetNight then setNight() end
    if _G.AutoBigHitbox then
        local hrp = LocalChar:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.Size = Vector3.new(25,25,25)
        end
    end
    if _G.AutoLockPosition then
        -- Optional: setup position lock
    end
end)

print("UI loaded! Features should now work.")
